import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
  container: {
    width: 40,
    height: 40,
  },
  img: {
    borderRadius: '100%',
    width: '100%',
    height: '100%',
  },
})

export default style
